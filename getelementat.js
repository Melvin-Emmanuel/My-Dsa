function getElementAt(arr, index) {
      return arr.at(index);
      }

      // Example:
      const myArray = [10, 20, 30, 40];

      console.log(getElementAt(myArray, 0));  // 10
      console.log(getElementAt(myArray, -1)); // 40
